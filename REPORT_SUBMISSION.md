# End2End 데이터 분석 프로젝트

**NYC Yellow Taxi Trip Data (2026-05) 분석 보고서**

| 항목 | 내용 |
|---|---|
| 과정 | AI의 서비스화 데이터분석 및 AIOps — 데이터 분석을 위한 Python 이해 |
| 반 | 9반 |
| 고유번호 | |
| 이름 | |
| 제출일 | 2026-08-09 |

---

## 1. 프로젝트 개요

NYC 옐로우 택시 2026년 5월 운행 기록 **전량 4,090,836행**에 대해 데이터 로드부터 보고서 자동 생성까지 전 과정을 구현했다. 분석 단계별로 클래스와 파일을 1:1 분리했고, `TaxiAnalysisPipeline`은 분석 로직 없이 실행 순서만 조정한다.

| 단계 | 클래스 | 역할 |
|---|---|---|
| 1 | `DataLoader` | 원본 다운로드, Pandas/Polars 비교 |
| 2 | `EDAAnalyzer` | 분포·결측·중복·품질 진단 |
| 3 | `StatisticalAnalyzer` | 원본 통계 검정 |
| 4 | `DataPreprocessor` | 근거 기반 정제 + Feature Engineering |
| 5 | `TaxiVisualizer` | Seaborn 정적 차트, Plotly 인터랙티브 |
| 6 | `StatisticalAnalyzer` | 정제 데이터 재검정 (전후 비교) |
| 7 | `ModelTrainer` | sklearn Pipeline 학습·평가·저장 |
| 8 | `ReportGenerator` | `report.md` 자동 생성 |

3단계와 6단계에 같은 클래스를 두 번 배치한 것은 의도된 설계로, **전처리 전후 동일 검정을 수행**해 처리가 결과를 어떻게 바꿨는지 비교하기 위함이다.

**설계 원칙은 전처리 규칙을 미리 정해두지 않는 것이다.** EDA와 통계 검정을 먼저 수행하고, 그 관찰값이 규칙을 선택하도록 구성했다. 선택된 규칙과 근거는 `artifacts/preprocessing_decisions.json`에 관찰값·처리·이유 형태로 기록된다.

---

## 2. 데이터 준비 — Pandas / Polars 비교

동일한 Parquet를 Pandas `read_parquet()`과 Polars `scan_parquet()` Lazy API로 각각 적재해 비교했다.

| 항목 | Pandas | Polars Lazy | 배수 |
|---|---:|---:|---:|
| 로딩 | 0.3354s | 0.0947s | 3.54× |
| GroupBy | 0.0877s | 0.0217s | 4.04× |
| 메모리 | 580.62MB | 550.13MB | 1.06× |

두 엔진을 경쟁이 아니라 역할로 나눠 썼다. Pandas는 `info()`·`describe()`·`isna()` 등 진단 API가 풍부해 데이터 점검에, Polars Lazy는 409만 행 전량의 분위수·품질 위반 집계에 사용했다.

로딩 배수는 원본 파일(66MB)의 OS 캐시 상태에 따라 크게 변동하므로 단일 배수를 근거로 삼지 않았다.

---

## 3. EDA와 데이터 품질

원본은 **4,090,836행 × 20열**, 완전 중복 **0건**이었다. 전처리가 필요한 문제는 세 가지다.

| 문제 | 관찰값 |
|---|---|
| 분석 월 이탈 | 픽업 시각 2008-12-31 ~ 2026-06-01 |
| 불가능한 값 | `trip_distance` 최대 307,491마일 / `average_speed_mph` 최대 1,511,821 mph |
| 음수 금액 | `fare_amount` -$950 / `total_amount` -$951 |

거리 중앙값은 1.87마일, p99는 19.40마일로 **분포 본체는 정상이고 극소수만 오염**된 형태였다. 한편 Location ID 범위 위반은 **0건**이어서 해당 필터는 활성화되지 않았다.

### 결측 패턴 — Flex Fare 운행

결측이 있는 5개 컬럼(`passenger_count`, `RatecodeID`, `store_and_fwd_flag`, `congestion_surcharge`, `Airport_fee`)의 결측 건수가 **모두 정확히 동일**했다 — 각 **955,371건(23.3539%)**. 교차 확인 결과 `payment_type = 0`인 행과 양방향으로 완전히 일치했다.

`payment_type = 0`은 TLC 공식 데이터 사전의 **Flex Fare trip**으로, 미터가 아닌 사전 확정 요금이다. 미터 기반으로 산정되는 항목이 이 요금제와 맞지 않는다는 점이 정황 설명이 되지만, **공식 문서는 이 필드들이 왜 결측인지까지 확정해 주지 않는다.** 요금이 부과되지 않아 비어 있는 것인지, 부과됐으나 기록되지 않은 것인지는 데이터만으로 구분할 수 없어 **기록 방식에 따른 구조적 결측으로 해석했다.**

이 집단은 나머지와 운행 성격도 달랐다.

| | 건수 | 평균 총액 | 거리 중앙값 |
|---|---:|---:|---:|
| 그 외 | 3,135,465 | $29.80 | 1.70 mi |
| Flex Fare | 955,371 | $32.74 | 2.63 mi |

---

## 4. 전처리 — 관찰값이 규칙을 선택

**IQR 일괄 제거 대신, p99와 최댓값의 격차가 10배를 넘는 경우에만** 도메인 상한을 적용했다. IQR만 쓰면 19마일 이상의 정상적인 공항·장거리 운행까지 제거되기 때문이다.

| 관찰 | 판정 | 결과 |
|---|---|---|
| 거리 p99 19.40 / max 307,491 | 10배 초과 | 도메인 상한 **활성화** |
| Location ID 위반 0건 | 위반 없음 | 필터 **비활성화** |
| `abs(Spearman) - abs(Pearson)` = 0.79 | 0.2 이상 | 이상치 왜곡 **판정** |

**결측 자체를 이유로 행을 삭제하지 않았고**, 품질 필터를 먼저 적용한 뒤 남은 행의 결측을 대체했다. `congestion_surcharge`는 Flex Fare 판정 시 `0`으로 채웠는데, 이 그룹에는 관측값이 하나도 없어 데이터로는 값을 정할 수 없기 때문이다. 다만 **이는 공식적으로 검증된 사실이 아니라 분석을 위한 가정이다** — 실제로 할증이 부과됐는데 기록만 누락됐을 가능성은 배제할 수 없다. 금액 가산에 중립적인 값을 택했다는 것이 실질적 근거다.

| 구분 | 값 |
|---|---|
| 전처리 전 | 4,090,836행 × 20열 |
| 전처리 후 | **3,899,516행 × 31열** |
| 제거 | 191,320행 (4.68%) |
| 잔여 결측 | **0건** |

열이 20 → 31로 늘어난 것은 운행시간·속도·시간대·주말 여부 등 파생 변수 11개를 생성한 결과다.

![전처리 근거 및 전후 비교](figures/preprocessing_evidence.png)

---

## 5. 시각화

![EDA 2x2 대시보드](figures/eda_overview_2x2.png)

Seaborn 2×2 대시보드에 거리 분포, 결제 유형, 시간대별 수요, 상관관계를 배치했다. 거리는 1~2마일에 집중된 오른쪽 꼬리 분포이고, 수요는 새벽 4~5시 최저·오후 6시 최고의 도시 통근 패턴을 보인다.

Plotly 인터랙티브 차트(`figures/hourly_demand_interactive.html`)에서는 정적 차트로 보이지 않던 차이가 드러난다. **주중은 오전 7~8시 급증 후 오후 6시 정점(약 17.5만 건)의 통근 패턴**인 반면, **주말은 새벽 0~2시 수요가 높게 유지되고 오전 급증 구간이 없다.** plotly.js를 파일에 포함해 네트워크 없이 열린다.

---

## 6. 통계 분석

원본과 정제 데이터에서 각각 고정 seed(42)로 200,000건을 추출해 동일한 검정을 수행했다.

| 검정 | 원본 | 정제 |
|---|---:|---:|
| Pearson (거리–운임) | **0.0104** | **0.8712** |
| Spearman (거리–운임) | 0.7971 | 0.8701 |
| Welch t-test (주중/주말) | t=18.85, p=3.80e-79 | t=17.56, p=6.25e-69 |
| 카이제곱 (결제유형–고액팁) | 85,226.75 | 83,098.67 |

**가장 큰 발견은 이상치가 상관계수를 뒤집었다는 것이다.** 원본 Pearson 0.0104는 "거리와 요금은 무관하다"는 결론을 주는데, 이는 `trip_distance = 307,491마일` 같은 극단값 하나가 계산을 지배한 결과다. 순위만 쓰는 Spearman은 같은 데이터에서 0.7971이었고, 정제 후 두 계수가 0.87로 수렴했다.

**t-test 해석.** 정제 데이터 기준 주중 평균 $31.06, 주말 평균 $29.28로 귀무가설을 기각한다. 다만 표본이 20만 건이라 작은 차이도 유의해지므로 효과 크기를 함께 봐야 한다 — 실제 차이는 $1.79로 주중 평균의 약 5.8%다.

**카이제곱 해석 시 주의.** `payment_type`과 고액 팁의 연관 강도는 0.64로 강하지만, TLC 데이터 사전상 `tip_amount`는 **카드 팁만 기록**한다. 따라서 이 연관은 팁 문화의 차이라기보다 기록 방식의 차이에 가깝다.

---

## 7. 머신러닝 Pipeline

`total_amount`를 목표로 하는 회귀 문제로 설정했다. 연속형 금액이므로 MAE·RMSE·R²를 사용했다(정확도·F1은 분류 지표라 정의되지 않는다).

**Target Leakage 방지가 핵심 설계였다.** `total_amount`는 정의상 `fare_amount` + `tip_amount` + 세금·할증의 합이므로, 이 컬럼들을 넣으면 모델은 예측이 아니라 덧셈을 학습해 R²가 1에 근접한다. 총액 구성 컬럼 9개와 목표 변수에서 파생된 `cost_per_mile`·`tip_percentage`를 제외해 입력을 13개로 구성했다.

다만 **간접적인 누수 가능성이 남아 있다.** `is_airport_trip`이 `RatecodeID == 2 또는 Airport_fee > 0`으로 생성되는데 `Airport_fee`는 위에서 제외한 총액 구성 항목이다. 즉 직접 제외한 컬럼이 파생 변수를 통해 우회 유입된다. 기여도는 +$0.30으로 작아 결론을 바꾸지 않지만 설계상 제외했어야 한다.

분할은 무작위 대신 **픽업 시각 정렬 후 앞 80% 학습 / 뒤 20% 평가**를 사용했다. 무작위 분할은 미래 시점 운행이 학습에 섞여 성능이 낙관적으로 측정되기 때문이다.

| 지표 | 값 |
|---|---:|
| MAE | **$3.238** |
| RMSE | $6.085 |
| R² | **0.9157** |
| 중앙값 baseline MAE | $12.864 |
| baseline 대비 개선 | **74.83%** |

변수 중요도는 한 변수를 무작위로 섞어 성능 저하를 보는 방식으로 측정했다. 단, 서로를 계산해낼 수 있는 변수 쌍(`is_weekend`←요일, `average_speed_mph`←거리÷시간)이 있어 하나씩 섞으면 중요도가 0으로 나온다. **중복 변수를 묶어서 함께 섞는 방식**으로 다시 측정했다.

| 변수 그룹 | 제거 시 MAE 증가 |
|---|---:|
| **거리 · 운행시간 · 속도** | **+$14.32** |
| `RatecodeID` | +$2.79 |
| `payment_type` | +$1.11 |
| 요일 · 주말 여부 | +$0.17 |

거리·시간 그룹이 압도적이며, 모델은 요금 구조를 알려주지 않았음에도 데이터에서 재구성했다.

---

## 8. 핵심 발견 — 주말은 더 빠르고 마일당 총액이 낮다

t-test에서 주말 총액이 유의하게 낮다는 것을 확인한 뒤, 격차와 함께 움직이는 패턴을 정제 데이터 전량에서 분해했다. 상식적인 설명은 "주말에는 짧게 타니까"지만 **결과는 반대였다.**

여기서 `마일당 총액 = total_amount ÷ trip_distance`이며, `total_amount`에는 미터 요금뿐 아니라 **팁·통행료·세금·할증이 모두 포함**된다.

| 항목 | 주중 | 주말 | 차이 |
|---|---:|---:|---:|
| 평균 총액 | $31.13 | $29.29 | -$1.85 |
| 평균 거리 | 3.49 mi | **3.58 mi** | **+2.6%** |
| 마일당 총액 | $12.56 | $10.91 | **-13.2%** |
| 평균 속도 | 9.95 mph | 11.36 mph | **+14.2%** |

주말 운행은 오히려 **2.6% 더 길다.** 총액과 함께 움직인 것은 거리가 아니라 마일당 총액이며, 같은 방향으로 평균 속도가 관찰된다.

교란 요인 세 가지를 통제해도 결과가 유지된다.

| 통제 | 결과 |
|---|---|
| 거리 구간 고정 | 네 구간 전부에서 격차 유지 (-11.5% ~ -1.4%) |
| 표준 미터 요금(`RatecodeID = 1`)만 | 속도 +13.6%, 마일당 총액 -11.5% |
| **팁 제외** | $11.11 → $9.79 (**-11.9%**) — 팁이 설명하는 몫은 1.3%p |

NYC 택시 표준 미터 요금은 거리와 시간을 함께 계산하므로, 정체가 심할수록 같은 거리에 더 많은 요금이 붙는다. 관찰 결과는 이 요금 구조와 정합적이다. **다만 경로·지역·할증 구성을 통제하지 않았으므로 정합성이지 인과는 아니다.**

---

## 9. 재현 방법과 한계

파이프라인이 생성하는 `reports/report.md`는 모든 수치가 실행 결과에서 직접 주입되며, Jinja2 `StrictUndefined`를 적용해 참조 값이 누락되면 빈칸 대신 즉시 오류가 발생한다. §8의 심화 분석 수치는 별도 스크립트가 `artifacts/`에 JSON으로 저장한다.

```bash
make setup   # 가상환경 생성 및 의존성 설치
make run     # 전체 파이프라인 (약 19~23초)
make test    # 단위 테스트

.venv/bin/python scripts/domain_analysis.py     # 운행 패턴 분석
.venv/bin/python scripts/model_diagnostics.py   # 모델 진단
```

재현성 장치는 고정 seed(42), 단계별 JSON/CSV 저장, 단위 테스트, 판단 근거를 남기는 `preprocessing_decisions.json` 네 가지다.

**한계.** ① 2026년 5월 단일 월이라 계절성을 확인할 수 없다. ② §8은 인과 추론이 아니다. ③ 도메인 상한값(거리 100마일, 시간 240분, 속도 80mph)의 발동 조건은 데이터 기반이지만 값 자체는 도메인 지식에서 왔다. ④ `congestion_surcharge`를 0으로 채운 것은 검증된 사실이 아니라 가정이다. ⑤ **모델은 배포 가능한 예측기가 아니다** — 기여도 상위 변수가 모두 운행 종료 후에야 확정되는데, 그 시점에는 미터기가 이미 금액을 알고 있다. 실제 요금 예측기를 만들려면 승차 전 정보(출발지·목적지·요일·시간대)만으로 다시 학습해야 한다.

---

## 10. 결론

409만 건 전량에 대해 데이터 적재부터 보고서 자동 생성까지 8단계를 클래스 단위로 구현했고, 전처리 규칙을 하드코딩하는 대신 **관찰값이 규칙을 선택하도록** 설계해 그 근거를 JSON으로 남겼다.

분석에서 얻은 결과는 두 가지다. 첫째, **이상치 하나가 상관계수를 뒤집을 수 있다** — 원본 Pearson 0.0104는 "거리와 요금은 무관하다"고 말했지만 정제 후 0.8712로 회복됐고, 단일 지표만 봤다면 정반대 결론에 도달했을 것이다. 둘째, **주말은 더 빠르고 마일당 총액이 낮으며**, 이 격차는 거리·요금제·팁을 통제한 뒤에도 유지되어 거리·시간 병산이라는 NYC 요금 구조와 정합적이었다.

다만 모델은 요금 구조를 재현했을 뿐 배포 가능한 예측기가 아니며, 주말 분석도 통제하지 않은 요인이 남아 인과가 아닌 정합성으로 읽어야 한다.

---

## 참고 자료

| 자료 | URL | 인용 |
|---|---|---|
| TLC Yellow Taxi Trip Records Data Dictionary — `payment_type` 코드 정의(0 = Flex Fare trip), `tip_amount`가 카드 팁만 포함 | `nyc.gov/assets/tlc/downloads/pdf/data_dictionary_trip_records_yellow.pdf` | §3, §6 |
| TLC E-Hail 사업자 안내 — Flex Fare가 사전 확정 비미터 요금 | `nyc.gov/site/tlc/businesses/e-hail-providers.page` | §3 |
| TLC Taxi Fare 규정 — 거리·시간 병산 구조 | `nyc.gov/site/tlc/passengers/taxi-fare.page` | §8 |
| TLC Trip Record Data — 원본 Parquet 배포처 | `nyc.gov/site/tlc/about/tlc-trip-record-data.page` | 전체 |
